# Use an in-memory H2 database for tests so tests don't try to connect to MySQL
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Let Hibernate create/update schema for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.flyway.enabled=false


# Use snake_case for JSON property names to match previous DTOs (firstname -> first_name)
spring.jackson.property-naming-strategy=SNAKE_CASE

